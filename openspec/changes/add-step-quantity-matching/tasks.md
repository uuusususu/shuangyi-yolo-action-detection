## 1. 配置模型

- [ ] 1.1 在配置对象中新增 `category_counts`，并保证旧配置默认数量为 1。
- [ ] 1.2 增加数量校验，配置步骤的数量不得小于 1。
- [ ] 1.3 保存配置时保持 `category_names` 与 `category_counts` 下标一致。

## 2. 配置界面

- [ ] 2.1 在动作类别序列每行增加数量输入框。
- [ ] 2.2 加载配置时把每个步骤数量填入对应输入框。
- [ ] 2.3 保存配置时写入每个步骤数量，并保持空类别步骤不参与判定。

## 3. 顺序状态机

- [ ] 3.1 扩展 `StepState` 保存 `required_count` 和当前帧 `current_count`。
- [ ] 3.2 将检测结果转换为当前帧类别计数，替代仅使用类别集合的命中判断。
- [ ] 3.3 当前步骤仅在当前帧数量严格等于目标数量时满足 PASS 条件。
- [ ] 3.4 保持不同帧数量不累计，稳定帧只累计连续满足数量条件的帧。
- [ ] 3.5 保持当前步骤优先和错序 NG 规则兼容。

## 4. 运行界面

- [ ] 4.1 在步骤卡片显示当前数量进度，例如 `3/4`。
- [ ] 4.2 步骤 PASS 后保留匹配数量上下文，不被后续画面刷新误改。
- [ ] 4.3 配置保存后重建步骤卡片和状态机，使类别数量立即生效。

## 5. 验证

- [ ] 5.1 增加配置加载/保存兼容测试，覆盖缺失 `category_counts` 的旧配置。
- [ ] 5.2 增加状态机测试，覆盖 4/4 PASS、3/4 等待、5/4 不 PASS。
- [ ] 5.3 增加状态机测试，覆盖 2 + 2 两帧不累计。
- [ ] 5.4 增加状态机测试，覆盖 PASS 稳定帧为 2 时必须连续两帧各自匹配。
- [ ] 5.5 运行相关 pytest 和 `openspec validate add-step-quantity-matching --strict`。
