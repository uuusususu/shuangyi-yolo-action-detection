## 1. 配置行为测试

- [x] 1.1 为 `ConfigManager` 增加默认 PASS 关闭、FAIL 开启的失败测试，并覆盖布尔值规范化。
- [x] 1.2 增加旧 `sound_feedback_enabled` 为 true/false 时迁移到 FAIL、PASS 保持关闭的加载测试。
- [x] 1.3 增加新字段优先于旧字段以及保存后重启恢复两个独立值的测试。
- [x] 1.4 增加配置页两个声音开关的加载、保存和互不影响测试。

## 2. 结果声音测试

- [x] 2.1 使用声音 spy 增加普通顺序模式测试：PASS 与 FAIL 分别受独立开关控制，同一 `round_id` 重复刷新只播放一次。
- [x] 2.2 增加首类别模式测试：同一父类同一轮 FAIL 的重复结果只播放一次。
- [x] 2.3 增加首类别模式测试：同一父类重新入队后的新轮次出现相同异常时再播放一次 FAIL 声音。
- [x] 2.4 增加两个不同父类分别 FAIL 时每轮各播放一次，以及 FAIL 开关关闭时完全静音的测试。
- [x] 2.5 增加声音播放器抛出异常时检测结果、红框状态、统计和下一轮调度仍继续的测试。

## 3. 独立声音配置实现

- [x] 3.1 在 `src/config.py` 增加 `pass_sound_enabled` 和 `fail_sound_enabled`，实现默认值、校验和旧字段迁移规则。
- [x] 3.2 在配置保存路径持久化两个新字段，并将旧兼容字段同步为 FAIL 开关值。
- [x] 3.3 在配置页“操作反馈”区域用“PASS 提示音”和“FAIL 提示音”两个开关替换统一声音开关。
- [x] 3.4 配置保存后即时更新声音能力，不关闭相机、不重建推理处理器、不清除父类状态。

## 4. 每轮一次的结果身份

- [x] 4.1 为首类别引擎的每次 ACTIVE 识别轮次分配唯一 `attempt_id`，并把它随 PASS/FAIL 结果传给 UI。
- [x] 4.2 让每个完成的 FAIL 轮次都暴露一次轮次结果，即使异常签名与旧轮次相同。
- [x] 4.3 将声音的轮次身份与异常签名首次上报信息分离，保持统计和证据保存的既有去重口径。
- [x] 4.4 调整普通顺序和首类别结果处理，分别读取 PASS/FAIL 开关并按结果轮次键最多播放一次。

## 5. 文档一致性

- [x] 5.1 更新 `yolo_action_detection/README.md`，说明两个声音开关、默认值、旧配置迁移和每轮一次规则。
- [x] 5.2 修订活动变更 `add-first-category-region-check` 中“异常签名不重复播放声音”的冲突描述，改为声音按轮次去重、统计和证据仍按异常签名去重。
- [x] 5.3 检查新增或修改的中文 Markdown，不保留 TBD、TODO、冲突默认值或含糊的“一次”定义。

## 6. 验证与交付

- [x] 6.1 运行配置、运行时 UI、首类别引擎和声音反馈相关的定向 pytest，确认新增测试全部通过。
- [x] 6.2 使用项目虚拟环境运行 `yolo_action_detection/tests` 完整测试集并记录通过数量。
- [x] 6.3 严格校验 `add-independent-result-sound-controls`，并重新严格校验同步修订后的 `add-first-category-region-check`。
- [x] 6.4 运行源码树 portable smoke，确认 PASS/FAIL 声音资源仍可解析且默认配置可加载。
- [x] 6.5 重新构建 `YOLOActionDetection` 可交付程序并运行 packaged `--portable-smoke-test`，确认新配置字段和声音资源进入打包产物。
