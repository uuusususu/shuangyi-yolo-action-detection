## 1. 配置与模式边界

- [ ] 1.1 先为 PCB 模式开关、PCB 类别、四个唯一元器件类别、FAIL 帧数、轮次间隔和归属容差编写配置加载、保存及非法值测试。
- [ ] 1.2 在 `ConfigManager` 和示例配置中实现 PCB 检查字段、默认值、类型转换与校验，确保旧配置默认保持原作业顺序模式。
- [ ] 1.3 为配置页保存和错误提示编写测试，再增加 PCB 检查模式、类别、FAIL 帧数及轮次间隔输入控件。

## 2. 元器件空间归属

- [ ] 2.1 为 OBB 扩张、中心点包含、交叠比例、板外目标和双 PCB 歧义场景编写失败测试。
- [ ] 2.2 实现 `pcb_inspection/geometry.py` 和 `ComponentOwnershipResolver`，保证一个元器件最多归属一块 PCB，且忽略元器件 tracker ID。
- [ ] 2.3 增加板边元器件、重复同类检测和多块保持间隔 PCB 的参数化测试，验证选择最高有效观测且不会跨板吸附。

## 3. 多 PCB 判定引擎

- [ ] 3.1 为首帧完整 PASS、连续三帧不完整 FAIL、第三帧恢复 PASS、部分入画不计 FAIL 和无 PCB ID 不判定编写引擎测试。
- [ ] 3.2 实现 `pcb_inspection/models.py` 与 `MultiPcbInspectionEngine` 的独立 PCB 状态、四槽位观测、连续 FAIL 和最终决策。
- [ ] 3.3 为同帧多 PCB 并行更新、已处理去重、未完成离场和短暂 ID 切换状态迁移编写测试并实现生命周期清理。
- [ ] 3.4 为按首次出现顺序排队、零间隔输出和非零间隔不阻塞观测编写时钟可控测试，并实现有界结果队列。

## 4. 运行时与界面集成

- [ ] 4.1 编写集成测试，证明每个待推理完整帧仍只调用一次现有模型处理器，且同一 overlay 可更新全部 PCB。
- [ ] 4.2 在主窗口中按模式路由 overlay：PCB 模式调用新引擎，原模式继续使用现有步骤状态机，并在配置重载后安全重建引擎。
- [ ] 4.3 将单次 PCB 最终结果接入现有声音、FAIL 证据和生产统计，记录 PCB ID、槽位状态、缺失类别、源帧与时间戳。
- [ ] 4.4 在画面叠加层显示 PCB ID、已识别元器件、连续 FAIL 帧和最终结果，确保已处理 PCB 仅显示、不重复反馈。

## 5. 验证与文档

- [ ] 5.1 运行 PCB 新增测试和现有完整测试集，修复行为回归并记录实际通过结果。
- [ ] 5.2 使用正常板、四种逐项缺件板和至少两块同时入画的视频验证单次推理、空间归属、三帧 FAIL、PASS、去重与轮次间隔。
- [ ] 5.3 检查长时间运行下状态和结果队列能够释放，确认推理延迟没有因同屏 PCB 数量产生额外模型调用。
- [ ] 5.4 更新中文 README 和配置说明，记录模型类别约束、PCB 必须保持间隔、完整入画条件、默认三帧 FAIL 及现场调参方法。
