## 1. 基线与测试护栏

- [x] 1.1 在不修改布局的前提下生成主界面和配置页六个分区的基线截图，并记录 1366×920、1920×1080 下现有控件边界问题。
- [x] 1.2 先增加主题初始化测试，验证主题在窗口创建前应用、使用深色模式和 `#35D7FF` 强调色，并在依赖缺失时明确失败。
- [x] 1.3 先增加标准控件测试，覆盖按钮静止边界、默认/悬停/按下/焦点/禁用状态以及禁止局部 `border: none` 覆盖。
- [x] 1.4 先增加下拉框、数字框和二元设置的渲染/行为测试，验证箭头、步进指示、选中/未选中/禁用状态及既有 PySide6 API。

## 2. 开源主题依赖与集中初始化

- [x] 2.1 在 `requirements.txt` 固定 `pyqtdarktheme==2.1.0`，确认 MIT 许可证并记录其运行依赖，不引入 GPLv3 Fluent 控件依赖。
- [x] 2.2 在 UI 层增加单一主题初始化入口，集中设置深色模式、圆角和项目青色强调色，并由 `main.py` 在创建窗口前调用。
- [x] 2.3 保留 `PAGE_BG`、`PANEL_BG`、文字色、描边色和步骤状态 token，确保主题库只接管标准交互控件而不覆盖容器与 PASS/FAIL 状态。
- [x] 2.4 检查 PyInstaller 分析结果；若自动收集不足，在现有 spec 中最小补充 `qdarktheme`、`darkdetect` 或所需资源，不修改迈德 SDK 包。

## 3. 配置页控件迁移

- [x] 3.1 移除配置页对 `input_style()` 的全局覆盖，让 `QLineEdit`、`QComboBox`、`QSpinBox` 和 `QDoubleSpinBox` 使用开源主题的边界、箭头和焦点状态。
- [x] 3.2 移除“返回检测”“浏览”“刷新设备”等按钮的无边框或重复自绘规则，使用主题标准次级按钮外观并保持现有尺寸、文案、信号和位置。
- [x] 3.3 调整 `ConfigNavButton`，保留 checkable 导航行为和六分区布局，同时让未选中、悬停、选中和键盘焦点状态清晰可辨。
- [x] 3.4 调整 `SwitchRow`，使用主题库绘制的标准 `QCheckBox` 指示器，保留 `stateChanged`、`isChecked()`、`setChecked()` 和右侧布局。
- [x] 3.5 调整 `FooterActionBar`，让“验证并保存”成为唯一主操作，“返回检测”为次级操作，保存成功提示和固定底栏行为保持不变。
- [x] 3.6 保留“归零并归档”的危险语义、非默认操作身份和确认对话框，不重新绘制标准按钮边框。
- [x] 3.7 运行并补齐配置页导航未保存值、配置加载/保存、首类别开关、PASS/FAIL 声音开关和统计归档回归测试。
- [x] 3.8 运行并补齐迈德相机下拉框回归测试，确认多相机显示、SN 选择、无 SN 项禁用、刷新和保存后切换行为不变。

## 4. 主检测页控件统一

- [x] 4.1 让顶部“配置”“关闭”和底部“打开相机”“开始检测”使用统一主题状态，保持主次/危险层级、现有尺寸和布局。
- [x] 4.2 删除主界面标准按钮上与主题冲突的重复边框绘制，仅保留必要的业务语义属性和项目容器样式。
- [x] 4.3 验证主题迁移不改变相机打开/关闭、检测开始/停止、配置入口、窗口关闭和运行时状态刷新行为。
- [x] 4.4 验证等待、检测中、PASS、FAIL、锁定、父类红框和检测叠加颜色不被主题覆盖，保存视觉配置不清空父类 ID、当前轮次或统计。

## 5. 真实视觉验收

- [x] 5.1 在 1366×920 渲染主界面和配置页六个分区，确认不悬停也能识别所有按钮、下拉框、数字框和二元设置。
- [x] 5.2 在 1920×1080 渲染同一组页面，确认文字、控件、帮助信息和固定底栏不裁切、不重叠、不溢出。
- [x] 5.3 分别捕获普通、悬停、按下、键盘焦点、选中和禁用状态，确认状态差异清晰且危险操作不会成为默认操作。
- [x] 5.4 在 Windows 100% 和 125% 显示缩放下检查打包程序，确认边界、箭头和中文文字清晰，布局保持现有深蓝/青色科技风。
- [x] 5.5 对比基线截图，确认只改变控件可操作性与细节，不改变主界面信息架构、配置页六分区结构、卡片位置和业务状态色。

## 6. 回归、文档与便携版交付

- [x] 6.1 更新项目 README 的 UI 技术栈、开源主题依赖、许可证说明和界面截图，不改写与本变更无关的检测文档。
- [x] 6.2 运行 UI、配置、相机、首类别、声音和运行时状态相关定向测试并确保全部通过。
- [x] 6.3 运行 `.venv\Scripts\python.exe -m pytest yolo_action_detection\tests -q` 并确保全量测试通过。
- [x] 6.4 运行源码 `--portable-smoke-test`，确认配置、模型、声音、证据目录和主题依赖均可用。
- [x] 6.5 运行 `openspec validate adopt-open-source-qt-control-theme --strict` 并修复所有规范错误。
- [x] 6.6 使用 `yolo_action_detection\packaging\build_yolo.bat` 重新生成便携版，确认主题模块和资源进入打包目录。
- [x] 6.7 运行打包 `YOLOActionDetection.exe --portable-smoke-test` 并确认返回码为 0。
- [x] 6.8 实际启动打包 EXE、打开配置页并完成最终截图验收，确认六个分区控件完整渲染且相机/检测入口可操作。
- [x] 6.9 记录最终 EXE 路径、构建时间、文件哈希、测试结果和未消除风险，交付用户验收后再进入 OpenSpec archive。
