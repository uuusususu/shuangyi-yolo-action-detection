## 1. 配置保存事件

- [x] 1.1 在 `ConfigPage` 增加配置保存成功信号，保存成功后发出事件。
- [x] 1.2 保存配置时立即写入当前配置文件，保存失败时提示错误并停止应用运行时配置。
- [x] 1.3 保持步骤类别输入完全由操作员手动填写，不读取模型类别覆盖 `category_names`。

## 2. 运行时模型重载

- [x] 2.1 在 `MainWindow` 监听配置保存事件，并判断模型路径或推理参数是否发生变化。
- [x] 2.2 模型相关配置变化时暂停推理，复用 `create_yolo_processor` 加载新处理器。
- [x] 2.3 新模型加载成功后原子替换 `self.processor` 和 `worker.frame_processor`。
- [x] 2.4 新模型加载失败时保留旧处理器，并在界面提示失败原因。
- [x] 2.5 如果保存前正在检测，模型重载成功后恢复检测状态。

## 3. 手动类别业务判断

- [x] 3.1 保存配置后重建 `StepSequenceEngine`，使手动配置类别和稳定帧立即生效。
- [x] 3.2 确认状态机继续使用 `detection.label` 与手动 `category_names` 进行匹配。
- [x] 3.3 确认未配置类别不会推进步骤，也不会自动新增或修改配置类别。

## 4. 验证

- [x] 4.1 增加或更新测试，覆盖模型路径变化后 `worker.set_frame_processor()` 被重新调用。
- [x] 4.2 增加或更新测试，覆盖模型加载失败时旧处理器被保留。
- [x] 4.3 增加或更新测试，覆盖保存配置不会从模型类别自动覆盖手动 `category_names`。
- [x] 4.4 运行相关测试和 OpenSpec 校验，确认提案要求满足。
